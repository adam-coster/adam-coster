export const scopeNames = Object.freeze(['source.yyp'] as const);
export const semanticTokens = Object.freeze([] as const);
export const tokens = Object.freeze({
	source: {
		json: {
			yyp: {},
		},
	},
} as const);
