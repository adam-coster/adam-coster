export const scopeNames = Object.freeze(['source.ignore'] as const);
export const semanticTokens = Object.freeze([] as const);
export const tokens = Object.freeze({
	comment: {
		line: {
			'number-sign': {
				ignore: {},
			},
		},
	},
} as const);
