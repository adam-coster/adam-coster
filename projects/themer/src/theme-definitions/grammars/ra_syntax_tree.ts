export const scopeNames = Object.freeze(['source.ra_syntax_tree'] as const);
export const semanticTokens = Object.freeze([] as const);
export const tokens = Object.freeze({
	constant: {
		numeric: {},
	},
	entity: {
		name: {
			class: {},
		},
	},
	string: {},
} as const);
